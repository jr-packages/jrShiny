---
title: "Interactive Graphics and shiny"
author: "Jumping Rivers"
date: "`r Sys.Date()`"
#output: revealjs::revealjs_presentation
output: ioslides_presentation
css: left.css
---
<!--
%\VignetteEngine{knitr::rmarkdown}
%\VignetteIndexEntry{Chapter 1 Slides}
-->

# Overview
## Course R package

```{r eval=FALSE}
install.packages("drat")
drat::addRepo("rcourses")
install.packages("nclRshiny")
library("nclRshiny")## Should work
```

If that works, try and get the dependencies
```{r eval=FALSE}
install.packages("nclRshiny", dependencies = TRUE)
```

<!-- ## Who? -->

<!-- * Dr Colin Gillespie -->
<!-- * Statistics Lecturer, Newcastle University -->
<!-- * Research interest in high performance and parallel computing -->
<!--   * Twitter: [csgillespie](https://twitter.com/csgillespie) -->
<!--   * Linkedin: [csgillespie](https://uk.linkedin.com/in/colin-gillespie-25028332) -->
<!-- * Book: [Efficient R programming](https://github.com/csgillespie/efficientR) O'Reilly -->

## Who am I?

  * Dr Jamie Owen
  * Lead trainer, Jumping Rivers
  
## Who are you?

## Slides 

All (except chapter 4) are in the package
```{r eval=FALSE}
vignettes("slides1", package="nclRshiny")
```
Exercise all in the package

```{r eval=FALSE}
vignettes("chapter1", package="nclRshiny")
```

# Chapter 1: What is markdown?
## What is markdown?

  * Markdown: lightweight markup language.
  * Plain and simple
    * [Github](github.com), wiki's, [stackoverflow](stackoverflow.com)
  * Simple and fast to write
    * I use it for agendas, minutes & reports

<!-- ## 1 minute task -->

<!--   * What features of Word do you often use? -->
  
## R markdown (.Rmd)

  * [R markdown](http://shiny.rstudio.com/articles/rm-cheatsheet.html) is a flavour of markdown 
    * Embed R code
  * Writing a document is quick
    * But some flexibility and polish is lost
  
## Headers

All R markdown files start with a header
```
---
title: "My title"
author: "Me"
date: "01 June 2016"
output: html_document
---
```

## Header: output

  * `html_document` 
  * `pdf_document` - on windows you need [miktex](http://miktex.org/)
  * `word_document`
  * `ioslides_presentation` -- a html format for presentations.
  * `beamer_presentation` -- pdf

## R markdown (.Rmd)

  * Really easy
    * This presentation was written in Markdown
  * Start a line with `#` creates a level $1$ header
    * Surrounding text with `**` will make the text **bold**.

# Exercise 1
## Exercise 1

```{r eval=FALSE}
vignette("chapter1", package="nclRshiny")
```

# Embedded R code with `knitr`
## Embedded R code with `knitr`

  * Supports multiple file types including markdown and LaTeX.
  * Allows a mixture of R and markdown
  
```{r echo=FALSE}
knitr::include_graphics("graphics/knit-logo.png")
```

## Chunks: Demo

## Chunks

  * R code is treated exactly the same as using the normal R GUI interpreter
* Variables can be used to store results
* Graphs can be created
* Use routines from various packages

## Chunk options

Option name | Value | Effect 
------------|-------|-------
`eval` | logical | Whether to evaluate a code chunk 
`echo` | logical | Whether to include the source code in output
`include` | logical | Whether to include the chunk results in output 
`fig.width`, `fig.height` | `numeric` | Dimensions of plots in inches


## Inline R code: Demo

## Processing a .Rmd file

  * Using RStudio, click `Knit HTML`
  * Or at the command line 
  
```{r eval = FALSE}
rmarkdown::render("file.Rmd")
```

# Exercise 2
## Exercise 2

```{r eval=FALSE}
vignette("chapter1", package="nclRshiny")
```

# Hyper Text Markup Language (HTML)
## Hyper Text Markup Language

  * `HTML` is a markup language for describing web pages
  * Whenever you go to a webpage, you are downloading an `HTML` document that your web-browser renders.
  * An `HTML` document is just a collection of tags:
    ```
    <tag>Some text</tag>
    ```
* Each tag, describes text formatting
* We translate the markdown file into HTML.
  * The markdown statement `**bold**` is converted into HTML statement
    ```
    <strong>bold</strong>
    ```

# Exercise 3
## Exercise 3

```{r eval=FALSE}
vignette("chapter1", package="nclRshiny")
```

